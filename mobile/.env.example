# Only needed when running on a physical device, which can't reach your
# machine via `localhost`. Set this to your machine's LAN IP instead, e.g.:
# EXPO_PUBLIC_API_URL=http://192.168.1.42:4000
#
# Android emulator and iOS simulator work without this (see src/api/client.ts).
EXPO_PUBLIC_API_URL=
